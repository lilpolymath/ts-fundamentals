interface Calculation {
  (a: number, b: number): number;
}

type CalculatorType = (x: number, y: number) => number;

const Add: CalculatorType = (a, b) => a + b;

const Sub: Calculation = (x, y) => x - y;

function handleMainEvent(elem, handler) {}

function handleMainEvent(elem, handler) {}

function handleMainEvent(elem, handler) {}
